machine:
  node:
    version: 7.7.0
dependencies:
  post:
    - yarn build
  override:
    - yarn
deployment:
  staging:
    branch: master
    heroku:
      appname: threadbare-staging
build:
  post:
    - mkdir $CIRCLE_ARTIFACTS/public
    - mv /public $CIRCLE_ARTIFACTS
general:
  artifacts:
    - "/public"